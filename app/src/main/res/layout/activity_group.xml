<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.illuminous.vittles.GroupActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@mipmap/groupblur"
        android:scaleType="centerCrop"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/round_linear_transparent"
        android:orientation="vertical"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true">

        <TextView
            android:id="@+id/text_find_group"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Create or Find a Group"
            android:textSize="32sp"
            android:layout_marginBottom="16dp"
            android:padding="16dp"
            android:textColor="@color/red"
            android:paddingTop="64dp"/>

        <EditText
            android:id="@+id/edit_find_group"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter a new group name or search existing groups."
            android:layout_below="@id/text_find_group"
            android:padding="16dp"
            android:textColorHint="@color/aluminum"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/round_button_red"
            android:layout_margin="8dp"
            android:text="Search"
            android:textColor="@color/white"
            android:onClick="searchGroup"/>

    </LinearLayout>



</RelativeLayout>
